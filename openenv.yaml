name: traffic-ai-env
version: 1.0

protocol: http
host: 0.0.0.0
port: 3000

endpoints:
  reset: /reset
  step: /step

observation_space:
  type: array
  shape: [5]

action_space:
  type: discrete
  n: 2