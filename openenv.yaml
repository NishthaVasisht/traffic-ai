name: traffic-ai-env
version: 1.0

entrypoint: inference.py

protocol: http

endpoints:
  reset: /reset
  step: /step

observation_space:
  type: array
  shape: [5]

action_space:
  type: discrete
  n: 2